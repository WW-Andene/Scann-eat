{
  "name": "scann-eat",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test --experimental-strip-types parser-tests.ts engine-tests.ts personal-score-tests.ts merge-tests.ts consumption-tests.ts",
    "dev": "node --experimental-strip-types server.ts",
    "build:web": "node build.mjs",
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "cap:build": "npm run build:web && npm run cap:sync"
  },
  "devDependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/cli": "^6.1.2",
    "@capacitor/core": "^6.1.2",
    "esbuild": "^0.24.0"
  }
}
